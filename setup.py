#!/usr/bin/env python
from setuptools import setup, find_packages
setup(
    name='omtf_reg',
    version='0.1',
    description='Regresion tool for OMTF',
    packages=find_packages(include=['omtf_reg']),
    install_requires=[
    ],
)
